[phases.setup]
nixPkgs = ["nodejs", "python312"]

[phases.install]
cmds = [
    "npm install",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = [
    "npm run build",
    "python manage.py collectstatic --noinput"
]

[start]
cmd = "gunicorn tourism_project.wsgi --log-file -"
